import React, { Component } from 'react';
import Router from '../router';

class Main extends Component {
  render() {
    return (
      <main>
        <Router/>
      </main>
    );
  }
}

export default Main;
